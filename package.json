{
  "name": "proctudy-presets",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "validate": "npx ajv validate -s schema.json -d \"presets/*.json\""
  },
  "dependencies": {
    "ajv-cli": "^5.0.0"
  }
}
